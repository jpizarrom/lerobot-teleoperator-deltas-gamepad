[project]
name = "lerobot_teleoperator_deltas_gamepad"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Juan Pizarro", email = "jpizarrom@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "av>=15.0.0,<16.0.0",
    "lerobot>=0.3.3",
]

[build-system]
requires = ["uv_build >= 0.8.17, <0.9.0"]
build-backend = "uv_build"